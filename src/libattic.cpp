#include "libattic.h"

#include "connectionmanager.h"
#include "tentapp.h"

static TentApp* pApp = 0;

TENTAPP StartupAppInstance(const char* szAppName, const char* szAppDescription, const char* szUrl, const char* szIcon, char* redirectUris[], char* scopes[])
{

    /*
    pApp = new TentApp();                                                
    app->SetAppName(std::string(szAppName));                    
    app->SetAppDescription(std::string(szAppDescription));   
    app->SetAppURL(std::string(szUrl));        

    
    app.SetScope(std::string());                    
    app.SetScope(std::string());                   

    app.SetRedirectURI(std::string());  
     */                                                           
    
    return 1;
}
